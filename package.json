{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/missionledger build",
  "outputDirectory": "artifacts/missionledger/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
